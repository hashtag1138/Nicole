# config_loader

But :

- montrer `Map<String,Int>` ;
- montrer des valeurs par defaut ;
- montrer `Result<Int,MapError>` ;
- comparer `case` et `result.unwrap-or`.

Fichier principal :

- [config_loader.nicole](config_loader.nicole)

Validation depuis la racine du depot :

```sh
../nicole_python_implementation/.venv/bin/python examples/validate_examples.py examples/config_loader
```
