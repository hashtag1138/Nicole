# collections

But :

- montrer des listes et des maps ;
- montrer `OutOfBounds` et `MissingKey` ;
- montrer `map.keys` et `map.values` ;
- rendre visible l'ordre des maps tel qu'il est confirme par la spec et les tests du depot.

Fichier principal :

- [collections.nicole](collections.nicole)

Validation depuis la racine du depot :

```sh
../nicole_python_implementation/.venv/bin/python examples/validate_examples.py examples/collections
```
